import React, { useContext } from 'react';
import { Context } from '../../Context/Context';

function Accounting() {

    //Retrieve products information from context
    const { Products, Commerce, dispatch } = useContext(Context);
    
    return (
        <div>
            
        </div>
    )
}

export default Accounting
